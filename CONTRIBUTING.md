# 개발 규칙

- 현재 도메인별 패키지 구조와 Service 인터페이스/구현체 분리를 유지한다.
- DTO는 record로 작성하고 구현 메서드에 @Override를 명시한다.
- 변수와 메서드는 의미 있는 camelCase, 타입은 PascalCase, 상수는 UPPER_SNAKE_CASE를 사용한다.
- 메서드는 동사로 시작하고 불필요한 축약어를 피한다.
- 주요 처리 단계는 짧은 한국어 주석으로 나누고, 설계 이유가 필요한 곳은 Why를 설명한다.
- 코드 자체로 충분한 단순 대입·접근자에는 주석을 반복하지 않는다.
- 미구현 표시는 `// TODO(윤진) : ...` 형식을 사용한다. 완료 범위의 필수 기능을 TODO로 남기지 않는다.
- 소스와 문서는 UTF-8로 저장한다.
- 에러 코드와 기본 메시지는 ErrorCode에서 관리하고 모든 오류는 동일한 ErrorResponse로 응답한다.
- Auth API는 합의된 `/auth` 경로를 유지한다. `/api` 접두사를 임의로 추가하지 않는다.

## Git

- API 하나를 구현하고 관련 검증을 완료할 때마다 로컬 커밋한다.
- 각 API 커밋에는 구현·관련 테스트·문서 변경을 포함한다.
- 여러 API의 완료 작업을 하나의 커밋으로 합치지 않는다.
- 공통 선행 기반과 도메인 공통 인가는 별도 커밋할 수 있다.
- 실패한 필수 테스트나 미구현 기능을 남긴 채 API 완료 커밋을 만들지 않는다.
- 커밋 메시지는 `feat: 회원가입 API 구현`처럼 변경 목적을 한국어로 설명한다.
- 사용자의 변경을 임의로 포함하거나 되돌리지 않는다. 요청 없이 amend·squash 등 이력을 변경하지 않는다.
- 사용자가 전체 결과를 확인하고 명시적으로 요청하기 전까지 **push하지 않는다**.
- 구현 완료·커밋 완료·테스트 성공은 push 승인으로 해석하지 않는다.
- 완료 보고에 API별 변경, 커밋 해시, 검증 결과, 남은 사항 및 미푸시 상태를 명시한다.

## 불명확한 사항

- 먼저 코드·명세·확정된 정책을 확인한다.
- 그래도 애매하거나 서로 충돌하면 사용자에게 근거·선택지·추천 이유를 설명하고 질문한다.
- 답변이 필요한 구현은 멈춘다. 답변과 무관한 확정 작업만 계속한다.
- 답변이 없다는 이유로 추천안을 자동 적용하지 않는다.
- API 계약·권한·데이터 구조·검증·의존성·범위 변경에도 같은 규칙을 적용한다.
